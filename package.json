{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd gui && npm install && npm run build",
    "start": "node main.js"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "cheerio": "^1.0.0-rc.3",
    "dotenv-flow": "^3.1.0",
    "express": "^4.17.1",
    "mongodb": "^3.3.2",
    "xlsx": "^0.15.1"
  }
}
